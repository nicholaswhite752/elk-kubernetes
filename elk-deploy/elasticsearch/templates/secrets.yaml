# Creates secret for any variables in extraEnvs
apiVersion: v1
kind: Secret
metadata:
  name: elasticsecret
type: Opaque
stringData:
  {{- range $name, $value := .Values.extraEnvs }}
  {{- if not (empty $value) }}
    {{ $name }}: {{ $value | quote }}
  {{- end }}
  {{- end }}